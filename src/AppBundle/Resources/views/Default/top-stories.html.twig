<div class="sidbar-box z-depth-1">
    <div class="sidebar-title">{{ 'Top Stories'|trans }}</div>
    <ul>
        {% for article in articles %}
            <li>
                <a href="{{ article.url }}"> {{ article.title }}</a>
            </li>
        {%  endfor %}
    </ul>
</div>