{% extends 'AppBundle::layout-bo.html.twig' %}

{% block content %}
    <div class="mdl-grid mdl-cell mdl-cell--9-col-desktop mdl-cell--12-col-tablet mdl-cell--4-col-phone mdl-cell--top">
        <div class="mdl-cell mdl-cell--11-col-desktop mdl-cell--11-col-tablet mdl-cell--4-col-phone ">
            <div class="mdl-card__title" style="position: relative">
                <h2 class="mdl-card__title-text">{{ 'Subscriber list' }}</h2>
            </div>
            <table class="mdl-data-table mdl-js-data-table mdl-data-table--selectable mdl-shadow--2dp projects-table">
                <thead>
                <tr>
                    <th class="mdl-data-table__cell--non-numeric">{{ 'Id'|trans }}</th>
                    <th class="mdl-data-table__cell--non-numeric">{{ 'Email'|trans }}</th>
                    <th class="mdl-data-table__cell--non-numeric">{{ 'Inscription date'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for subscriber in subscribers %}
                    <tr>
                        <td class="mdl-data-table__cell--non-numeric">
                            {{ subscriber.id }}
                        </td>
                        <td class="mdl-data-table__cell--non-numeric">
                            {{ subscriber.email }}
                        </td>
                        <td class="mdl-data-table__cell--non-numeric">
                            {{ subscriber.createdAt|date('d/m/Y') }} à {{ subscriber.createdAt|date('H:i') }}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}